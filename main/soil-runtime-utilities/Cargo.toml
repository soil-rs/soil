[package]
name = "soil-runtime-utilities"
version = "0.2.0"
license.workspace = true
authors.workspace = true
edition.workspace = true
homepage.workspace = true
repository.workspace = true
description = "Soil client utilities for frame runtime functions calls"

[lints]
workspace = true

[package.metadata.docs.rs]
targets = ["x86_64-unknown-linux-gnu"]

[dependencies]
codec = { workspace = true, default-features = true}

soil-client = { workspace = true, default-features = true}
subsoil-crypto-hashing = { workspace = true, default-features = true}
subsoil = { workspace = true, default-features = true}

thiserror = { workspace = true}

[dev-dependencies]
subxt = { workspace = true, features = ["native"] }

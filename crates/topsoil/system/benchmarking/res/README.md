These runtimes are used for benchmarking the `set_code` intrinsic.

**Don't use them in production environments!**

To update the just copy the new runtime from
`target/release/wbuild/soil-test-staging-node-runtime/soil_test_staging_node_runtime.compact.compressed.wasm` to here.

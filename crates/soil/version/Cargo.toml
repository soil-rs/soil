[package]
name = "soil-version"
version = "29.0.0"
authors.workspace = true
edition.workspace = true
license.workspace = true
homepage.workspace = true
repository.workspace = true
description = "Version module for the Substrate runtime; Provides a function that returns the runtime version."
documentation = "https://docs.rs/sp-version"
readme = "README.md"

[lints]
workspace = true

[package.metadata.docs.rs]
targets = ["x86_64-unknown-linux-gnu"]

[dependencies]
codec = { features = ["derive"], workspace = true }
impl-serde = { optional = true, workspace = true }
parity-wasm = { optional = true, workspace = true }
scale-info = { features = ["derive"], workspace = true }
serde = { features = ["alloc", "derive"], optional = true, workspace = true }
soil-crypto-hashing-proc-macro = { workspace = true, default-features = true }
soil-runtime = { workspace = true }
soil-std = { workspace = true }
soil-version-proc-macro = { workspace = true }
thiserror = { optional = true, workspace = true }

[features]
default = ["std"]
std = [
	"codec/std",
	"impl-serde/std",
	"parity-wasm",
	"scale-info/std",
	"serde/std",
	"soil-runtime/std",
	"soil-std/std",
	"thiserror",
]

# Serde support without relying on std features.
serde = ["dep:serde", "impl-serde", "soil-runtime/serde"]
